struct __dynamic_array  // 24 B
{
    s64 Length;         // 8 B
    @f32 Data;          // 8 B
    s64 Size;           // 8 B

    // f32 __index(s64 index)
    // {
    //     return $@f32(@void(s64(Data) + TypeSize(f32) * index));
    //  // return $@f32(@void(s64(@Data) + TypeSize(f32) * index));
    //  // return $@f32(@Data + TypeSize(f32) * index);                         // @Data + N -> @void
    //  // return (Data + sizeOf(float) * index)*
    // }
}

// struct __better_dynamic_array_f32   // 24 B
// {
//     s64 Length;                     // 8 B
//     @[]f32 Data;                    // 8 B
//     s64 Size;                       // 8 B

//     f32 __index(s64 index)
//     {
//         return ($Data)[index];
//     }
// }

@void AllocB(s64 size)
{
    return __th_allocB(size);
}

@void ReallocB(@void ptr, s64 newSize)
{
    return __th_reallocB(ptr, newSize);
}


void Free(@void ptr)
{
    __th_free(ptr);
    return;
}

void TypeSize()
{
    return;
}