struct Vector3(float x, float y, float z);

union StringData(int i, float f);

int main()
{
    m(7);

    int t = m(8);
    int u = 1 + m(8) + 9;

    Entity entity = new Entity(70.0, 1.5);

    float health = entity.health;
    float moreHealth = 1.0 + entity.health;

    int defaultInt;
    float defaultFloat;
    bool defaultBool;

    s32 default_s32;
    f32 default_f32;
    bool default_bool;

    f128 quad;

    int i = -1;
    float j = -1.0;

    bool fg = false;
    bool ffg = true;
    int a = 5;
    int b = 10;
    int c = a + b;

    int d = 4;
    d = a + c;

    d = d + 42;

    float f = 2.5;
    f = f * 2.0;

    float g = 3.0;
    float h = f - g;

    bool ok = c > 5;
    return c;
}

int m(int j)
{
    int i = 3;
    j = 4;
    int health = 7;
    return 0;
}

struct Entity(float health, float speed)
{
    bool IsAlive()
    {
        bool alive = health > 0.0; 
        return alive;
    }
}
